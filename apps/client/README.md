# Foods Client

饮食生活助手的 Flutter 客户端，移动端优先，并保留 Windows 与 macOS 工程。

```powershell
flutter pub get --enforce-lockfile
flutter run -d <device-id>
flutter analyze
flutter test
```

当前识别流程使用本地素材模拟异步结果；相机、持久化、同步和服务端业务接口将在后续迭代接入。
